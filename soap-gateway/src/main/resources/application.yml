spring:
  application:
    name: soap-gateway
zuul:
  routes:
    system-service:
      path: /system/**
      service-Id: system-service
management:
  endpoints:
    web:
      exposure:
        include:  ["routes"]
